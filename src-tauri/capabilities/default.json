{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. The webview navigates to http://127.0.0.1:<random-port>/ after the sidecar binds, so we have to declare 127.0.0.1 / localhost as trusted remote origins for plugin commands to be callable.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "updater:default",
    "process:allow-restart",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        { "name": "binaries/cud-server", "sidecar": true, "args": true }
      ]
    }
  ]
}
