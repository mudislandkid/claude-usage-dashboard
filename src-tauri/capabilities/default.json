{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        { "name": "binaries/cud-server", "sidecar": true, "args": true }
      ]
    }
  ]
}
